# Phase 1 — CRUD Foundation

**Depends on:** nothing (this is the foundation phase).
**Unblocks:** Phases 2–6 all reuse the form/dialog primitives and `create`/`update`/`remove` API methods built here.

## 1. Goal

Every one of the 12 fully-CRUD-capable resources currently only supports browsing (search/list) in the UI, even though the backend supports full create/edit/delete. Close that gap, and build the shared infrastructure so later phases don't re-solve the same problem per module.

**Resources in scope:** Bills, Categories, Inventory, ItemReturns, Notifications, Organizations, PaymentTransactions, Products, PurchaseOrders, ReportGenerationLogs, Stores, Suppliers.

(PurchaseOrders gets its *basic* CRUD here; line items, linked Bills/Payments, and the full purchase workflow are Phase 2.)

## 2. Current state (verified in code)

- `ERPDataTable` (`components/ERPDataTable.tsx`) already accepts `onAdd`/`onEdit`/`onDelete` props and renders Add/Edit/Delete buttons when they're supplied — but every current caller either omits them or wires them to `console.log`.
- `ModulePage.tsx` (the actual routed generic table for these resources via `App.tsx` `GENERIC_KEYS`) has an inert "Add New" button and inert per-row "Edit" button — no handlers at all, and no delete action rendered.
- `api.ts` `makeResource<T>()` exposes only `search`, `list`, `getById`. No mutation methods exist.
- `components/ui/` has `button.tsx`, `input.tsx`, `label.tsx` only. No dialog/modal, no select, no textarea, no form-error display.

## 3. Data model per resource (fields from `types.ts`, cross-checked against OpenAPI path existence — field-level detail in the OpenAPI schemas themselves is unreliable/under-annotated, so `types.ts` is treated as ground truth here)

| Resource | Fields (all optional unless noted) | Notes |
|---|---|---|
| Organization | name*, code, email, phone, status | |
| Store | name*, code, address, organization_id, status | Spec wants `type` (Warehouse/Branch) and `state` — **blocked**, not in backend. |
| Category | name*, code, description, parent_id, status | `parent_id` implies hierarchy — confirm whether UI needs a tree/nested picker or flat dropdown (see §6). |
| Product | name*, code, unit, unit_price, sku, barcode, category_id, status | `unit` is free text, not a Unit Library reference — spec's multi-unit-variant model is **blocked**. |
| InventoryItem | product_id, store_id, quantity, min_quantity, unit, status | This is a stock-balance record per product+store, not a transaction; edits here should probably be restricted to `min_quantity`/`status` — raw `quantity` edits should go through Stock Movements (Phase 4) to keep an audit trail. Flag as a design decision, not a hard rule — see §6. |
| Supplier | name*, code, email, phone, status | Spec wants credit_limit/credit_days/opening_balance — **blocked**. |
| PurchaseOrder | supplier_id, store_id, total_amount, status, ordered_at | `status` values unknown from schema (renderer's dead `PurchaseOrdersView.tsx` implies `pending/approved/received/cancelled` — unverified against live enum, confirm via a real create call before hardcoding a `<Select>` list). |
| Bill | purchase_order_id, amount, due_date, status | |
| PaymentTransaction | reference, type, amount, status | Live OpenAPI schema shows a richer shape at the endpoint level (`orgId`, `referenceId`, `referenceType`, `method`) than `types.ts` currently models — reconcile field names against a real response before building the form, don't guess. |
| Notification | title, message, type, read, created_at | Likely system-generated (from backend events) more than user-created — confirm whether "Add" even makes sense here, or if only "mark read"/delete are real actions (see §6). |
| ItemReturn | return_number, status, total_amount | Spec's `returnType` (sales vs purchase) and links (`orderId`/`supplierId`) exist in the OpenAPI request schema but aren't in `types.ts` yet — add them; this field is what makes ItemReturn double as both Sales Return (Phase 3) and Purchase Return (Phase 2). |
| ReportGenerationLog | report_type, status | Likely system-generated by a report job, not user-authored — confirm before building a create form (see §6). |

\* = required to create, per common sense / non-nullable in `types.ts`; not independently verified against backend validation rules since no live token was available this session.

## 4. Screens & interactions

For each resource:
1. **List** (already exists) — keep as-is, just confirm it's the routed `ModulePage`/`ERPDataTable` version, not a dead `*View.tsx`.
2. **Create** — "Add" button opens a Dialog with a form built from the resource's editable fields. On submit: `Api.<Resource>.create(body)`, invalidate the TanStack Query cache for that `queryKey`, close dialog, toast success (project already has `sonner` installed for this).
3. **Edit** — row "Edit" button opens the same Dialog pre-filled via `getById` (or the row data already in hand), submits via `Api.<Resource>.update(id, body)`.
4. **Delete** — row "Delete" button opens a `<ConfirmDialog>` ("Delete this {resource}? This can't be undone."), submits via `Api.<Resource>.remove(id)`.
5. Client-side validation: required fields (marked * above), numeric fields reject non-numeric input, no other business validation is knowable without backend error responses — surface backend 400 error bodies verbatim in the form rather than guessing validation rules.

## 5. Foundation work (build once, here)

1. **`api.ts` mutation methods** — extend `makeResource<T>`:
   ```ts
   async create(body: Partial<T>): Promise<T> { return post<T>(basePath, body); }
   async update(id: string, body: Partial<T>): Promise<T> { /* needs a `put<T>()` helper added next to get/post */ }
   async remove(id: string): Promise<void> { /* needs a `del()` helper */ }
   ```
   Add `put<T>()` and `del()` helpers next to the existing `get`/`post` functions. **Only attach `update`/`remove` to the 12 resources that support them** — for the other 13 (Phase 2/3/4 territory), exporting `.update()`/`.remove()` would be dead code that silently 404s if ever called.
2. **`components/ui/dialog.tsx`** — thin wrapper over `@radix-ui/react-dialog` (already a dependency), styled to match the existing dark theme (`var(--surface-1)` etc. used elsewhere).
3. **`components/ui/select.tsx`** — wrapper over `@radix-ui/react-select` (already a dependency), for status/enum fields.
4. **`components/ResourceForm.tsx`** (or similar) — a generic form renderer driven by a field-config array per resource (name, label, type: text/number/select/date, required, options). Reuses the same shape `ERPDataTable`'s `Column<T>` already uses, so table and form definitions can live next to each other per resource.
5. **`components/ConfirmDialog.tsx`** — generic yes/no confirmation, reused for every delete action app-wide.
6. Wire `create`/`update`/`remove` into `ModulePage.tsx` (or replace it per-resource with dedicated pages using `ERPDataTable` + the new form, matching the `Products.tsx` pattern — decide during implementation which is less duplicative given 12 resources).
7. Fix `Api.Users` (add a `PlatformUser` type distinct from the existing local-SQLite `User` type, since `/api/v1/users` exists on the backend but has no client wrapper — name collision with the existing local `User` interface used for PIN-based operator accounts must be avoided).

## 6. Open questions for this phase

- **Category `parent_id`**: does the business actually use nested categories, or is this a flat list where `parent_id` is vestigial? Affects whether the create form needs a tree-picker or a simple dropdown.
- **InventoryItem edits**: should raw `quantity` be directly editable here, or only via Stock Movements (Phase 4) so every stock change has a reason/audit trail? Recommend the latter — restrict this screen's edit form to `min_quantity`/`status` and route quantity changes through Phase 4's Stock Adjustment screen once it exists. Confirm before building.
- **Notifications / ReportGenerationLogs**: likely system-generated, not user-created. Confirm whether "Add" should exist at all for these two, or whether the phase should only add Edit (mark-as-read) / Delete.
- **PurchaseOrder status enum & PaymentTransaction field names**: the OpenAPI schema is too thin to trust here. Before building the `<Select>` options / form fields for these two, make one real authenticated GET/POST against the live API to confirm actual field names and enum values rather than guessing from dead legacy component code.

## 7. Done when

- [ ] `api.ts` has `put`/`del` helpers and `create`/`update`/`remove` on the 12 full-CRUD resources.
- [ ] Dialog, Select, ResourceForm, ConfirmDialog components exist in `components/ui/`.
- [ ] All 12 resources: Add/Edit/Delete work end-to-end against the real API, with query cache invalidation and toast feedback on success/failure.
- [ ] `Api.Users` wired (even if its own screen build-out is Phase 5's job — at minimum stop it from silently rendering empty).
- [ ] Dead `components/*View.tsx` files deleted or explicitly kept for a stated reason.
